# SELECTORES OPTIMIZADOS PARA AVIANCA
# Generados automáticamente por explore_page_structure.py

from selenium.webdriver.common.by import By

class OptimizedSelectors:
    ORIGIN_INPUT = (By.XPATH, "//input[@placeholder]")
    LANGUAGE_SPANISH = (By.XPATH, "//*[contains(@class, 'language')]")
